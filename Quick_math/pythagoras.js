// Pythagoras theorem

calcHypotenuse = (a, b) => Math.sqrt( (a * a) + (b * b) );

calcHypotenuse = (a, b) => (Math.sqrt( (a**2) + (b**2)));

function calcHypotenuse(a, b) {
  return Math.sqrt( (Math.pow(a,2) + Math.pow(b,2)) );
}


function calcHypotenuse(a, b) {
  return Math.sqrt( (Math.pow(a,2) + Math.pow(b,2)) );
}

calcHypotenuse(1,1);; // 1.4142135623730951


// Math.sqrt(2);
// 1.4142135623730951

function Hypot(a, b) {
  return Math.hypot(a, b);
}
