/**
 * Function: simulate a cash register
 * @params1: purchase price amount
 * @params2: payment cash amount
 * @params3: available change in cash register draw,
 * 2d array i.e [["FIVE", 55], ["TEN", 70]]
 *
 * Return {status: "INSUFFICIENT_FUNDS", change: []} if cash-in-drawer is less than the change due, or if you cannot return the exact change.

 * Return {status: "CLOSED", change: [...]} with cash-in-drawer as the value for the key change if it is equal to the change due.

 * Return {status: "OPEN", change: [...]}, with the change due in coins and bills, sorted in highest to lowest order, as the value of the change key.
 */

function checkCashRegister(price, cash, cid) {}

let cid = [["FIVE", 55], ["TEN", 70]];
let price = 15;
let cash = 20;


let change =


cid =
